apiVersion: v1
kind: Service
metadata:
  name: aura-api-analyzer
  labels:
    app.kubernetes.io/name: aura-api-analyzer
spec:
  selector:
    app.kubernetes.io/name: aura-api-analyzer
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: http
